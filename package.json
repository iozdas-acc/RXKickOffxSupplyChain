{
  "name": "webcontext",
  "version": "1.0.0",
  "description": "",
  "main": "onboard.js",
  "scripts": {
    "dev": "cd site && next dev",
    "build": "cd site && next build",
    "start": "cd site && next start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
