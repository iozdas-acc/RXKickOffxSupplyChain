Run a full project health check and initialise anything missing. Follow every step in order — do not skip.

## Step 1 — Read current state
Read these files before doing anything else:
- `memory/MEMORY.md`
- `memory/next.md`
- `memory/issues.md`

## Step 2 — Check context gates (in order)

### Gate A: .impeccable.md
- File must exist AND have content beyond the template headers.
- If missing or empty: tell the user to run `node onboard.js` or fill it manually. STOP — do not proceed past this gate until fixed.

### Gate B: .agents/product-marketing-context.md
- File must exist AND have real content.
- If missing or empty: same as Gate A. STOP.

### Gate C: memory/sitemap.md
- File must exist AND have real content (not just the "not yet generated" placeholder).
- If missing or placeholder: tell the user: "Site architecture hasn't been planned yet. Run `/plan` first — it will map all pages, define the content strategy, and scaffold the page folders."
- STOP until resolved. Do not proceed past this gate.

### Gate D: design-system/MASTER.md
- File must exist. This is generated by running `ui-ux-pro-max --design-system --persist`.
- If missing: tell the user you need to generate it now. Ask: "Ready to run the design system generation? This reads your brand context and generates the token architecture."
- If they say yes: use the `ui-ux-pro-max` skill with `--design-system --persist`. Write output to `design-system/MASTER.md`.
- If they say no: log this as a blocker in `memory/issues.md` (I-NNN) and add to `memory/next.md`. STOP.

### Gate E: Pages registered
- Check `memory/MEMORY.md` pages table. All pages should be listed.
- If any page folder exists in `pages/` but is not in MEMORY.md, register it.
- If any page is in `memory/sitemap.md` but has no folder in `pages/`, flag it — the user should run `/plan` to scaffold missing folders.

### Gate F: Asset manifest
- Check `assets/raw/` directory.
- List any files in `assets/raw/` (including subdirectories) that do NOT have a corresponding entry in `assets/manifest.md`.
- If unregistered files exist: report them. Ask if user wants to register them now (or tell them to run `/sync-assets`).

### Gate G: Blocking issues
- Read `memory/issues.md`.
- If any issue has `Status: open` and `Severity: blocking` — surface it clearly. These must be resolved before design or build work.

## Step 3 — Report

Print a clean status table:

```
GATE             STATUS    NOTES
.impeccable.md   ✅/❌     [detail if failed]
product-mktg     ✅/❌     [detail if failed]
sitemap          ✅/❌     [page count if passing]
design-system    ✅/❌     [detail if failed]
pages            ✅/❌     [count registered]
assets           ✅/❌     [count unregistered if any]
blockers         ✅/❌     [count open blocking issues]
```

Then print the contents of `memory/next.md` (Now section only).

## Step 4 — Update memory
After running all checks, update `memory/next.md` to clear any completed items and add any new gaps found.
